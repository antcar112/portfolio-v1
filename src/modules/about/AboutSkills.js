import React from 'react'
import { SectionTitle, TechGrid } from '@src/ui/components'

export const AboutSkills = () => (
  <section>
    <SectionTitle center>My Skills</SectionTitle>
    <TechGrid showInProgress />
  </section>
)
